// Tipos de dados Javascript

// declarando Strings
const nome = 'meu nome'
const nome2 = "meu nome"
const nome3 = `meu nome`

// Declarando numbers
const num1 = 12
const num2 = 12.6

// Declarando undefined
let nomeAluno // não aponta para local nenhum na memória

// Declarando null
let sobrenomeDoAluno = null // não aponta para local nenhum na memória, mas não é igual ao undefined

// Declarando boolean
const aprovado = true // ou false (lógico)